{
  "name": "learntoread",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "generate:phonemes": "node scripts/generate-phonemes.mjs"
  },
  "devDependencies": {
    "klattsch": "^0.8.0",
    "wrangler": "^4.118.0"
  }
}
